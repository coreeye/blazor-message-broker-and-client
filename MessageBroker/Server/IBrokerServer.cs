using MessageBroker.Shared;
using System;

namespace MessageBroker.Server
{
    internal interface IBrokerServer
    {
        //void StartServer();

        //void StopServer();

        //bool IsRunning();

        //void AddResponse(Guid clientId, Response response);
    }
}